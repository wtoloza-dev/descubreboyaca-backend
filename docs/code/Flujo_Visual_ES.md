# 🎨 Flujo Visual: Lifespan + Dependency Injection

## 📺 Diagrama Animado (imagina que se mueve)

```
════════════════════════════════════════════════════════════════════
                    INICIO DE LA APLICACIÓN
════════════════════════════════════════════════════════════════════

Terminal: $ uvicorn app.main:app
    │
    ▼
┌─────────────────────────────────────────────────────────────────┐
│  LIFESPAN STARTUP (se ejecuta UNA VEZ)                          │
│  ─────────────────────────────────────────────────────────────  │
│                                                                  │
│  1. FastAPI detecta: app = FastAPI(lifespan=lifespan)          │
│  2. Ejecuta: lifespan(app)                                      │
│     │                                                            │
│     ├─ print("🟢 Creando adapter...")                          │
│     ├─ _async_adapter = crear_adapter()  ← COSTOSO             │
│     │  └─ Crea engine                                           │
│     │  └─ Configura connection pool                             │
│     │  └─ Inicializa recursos                                   │
│     │                                                            │
│     └─ print("✅ Adapter listo")                               │
│                                                                  │
│  _async_adapter ahora vive en memoria global                   │
│  (será compartido por TODOS los requests)                      │
└─────────────────────────────────────────────────────────────────┘
    │
    ▼
┌─────────────────────────────────────────────────────────────────┐
│  APLICACIÓN CORRIENDO                                           │
│  ─────────────────────────────────────────────────────────────  │
│                                                                  │
│  FastAPI está escuchando en http://localhost:8000              │
│  Esperando requests...                                          │
│                                                                  │
│  ┌──────────────────────────────────────────────────────────┐  │
│  │ MEMORIA GLOBAL                                            │  │
│  │                                                            │  │
│  │  _async_adapter = <SQLiteAsynchronousAdapter>            │  │
│  │    ├─ engine (vivo y listo)                              │  │
│  │    └─ connection_pool (5 conexiones)                     │  │
│  └──────────────────────────────────────────────────────────┘  │
└─────────────────────────────────────────────────────────────────┘
    │
    │ (La app puede correr por horas/días/meses aquí)
    │
    ├──────────────────────────────────────────────────────────────
    │  REQUEST 1: GET /api/v1/restaurants
    ├──────────────────────────────────────────────────────────────
    │
    ▼
┌─────────────────────────────────────────────────────────────────┐
│  DEPENDENCY INJECTION                                           │
│  ─────────────────────────────────────────────────────────────  │
│                                                                  │
│  FastAPI ve: session = Depends(get_async_session_dependency)   │
│  FastAPI llama: get_async_session_dependency()                 │
│     │                                                            │
│     ├─ adapter = get_async_adapter()                           │
│     │  └─ return _async_adapter  ← El que creamos en startup  │
│     │                                                            │
│     ├─ async with adapter.get_session() as session:            │
│     │  └─ Obtiene una conexión del pool                        │
│     │  └─ Crea session temporal                                │
│     │                                                            │
│     └─ yield session  ← Se la pasa al endpoint                 │
└─────────────────────────────────────────────────────────────────┘
    │
    ▼
┌─────────────────────────────────────────────────────────────────┐
│  ENDPOINT HANDLER                                               │
│  ─────────────────────────────────────────────────────────────  │
│                                                                  │
│  async def get_restaurants(session: AsyncSession):             │
│      result = await session.exec(select(Restaurant))           │
│      return result.all()                                        │
│                                                                  │
│  [Ejecuta la query usando la session]                          │
│  [Obtiene los restaurantes]                                    │
│  [Retorna los datos]                                           │
└─────────────────────────────────────────────────────────────────┘
    │
    ▼
┌─────────────────────────────────────────────────────────────────┐
│  CLEANUP AUTOMÁTICO                                             │
│  ─────────────────────────────────────────────────────────────  │
│                                                                  │
│  La función get_async_session_dependency termina:              │
│     async with adapter.get_session() as session:               │
│         yield session                                           │
│     # ← Aquí se cierra la session automáticamente             │
│                                                                  │
│  └─ session.close()                                            │
│  └─ Conexión regresa al pool                                   │
│  └─ _async_adapter sigue vivo para el próximo request         │
└─────────────────────────────────────────────────────────────────┘
    │
    ▼
┌─────────────────────────────────────────────────────────────────┐
│  RESPONSE                                                        │
│  ─────────────────────────────────────────────────────────────  │
│                                                                  │
│  FastAPI envía: [{"id": 1, "name": "Restaurant 1"}, ...]      │
│  Status: 200 OK                                                 │
└─────────────────────────────────────────────────────────────────┘
    │
    │ (El adapter sigue vivo en memoria)
    │
    ├──────────────────────────────────────────────────────────────
    │  REQUEST 2: GET /api/v1/restaurants
    ├──────────────────────────────────────────────────────────────
    │
    │  [Repite el proceso desde "DEPENDENCY INJECTION"]
    │  [USA EL MISMO ADAPTER - no crea uno nuevo]
    │  [Solo crea una nueva session]
    │
    ├──────────────────────────────────────────────────────────────
    │  REQUEST 3, 4, 5... N
    ├──────────────────────────────────────────────────────────────
    │
    │  [Todos usan el mismo adapter]
    │  [Cada uno tiene su propia session temporal]
    │  [Rápido y eficiente]
    │
    │
    ▼
┌─────────────────────────────────────────────────────────────────┐
│  CIERRE DE LA APLICACIÓN                                        │
│  ─────────────────────────────────────────────────────────────  │
│                                                                  │
│  Terminal: Ctrl+C                                               │
│      │                                                           │
│      ▼                                                           │
│  LIFESPAN SHUTDOWN (se ejecuta UNA VEZ)                        │
│      │                                                           │
│      ├─ print("🔴 Cerrando adapter...")                       │
│      ├─ await _async_adapter.engine.dispose()                  │
│      │  └─ Cierra todas las conexiones                         │
│      │  └─ Libera recursos                                     │
│      │                                                           │
│      └─ print("👋 Aplicación cerrada")                         │
│                                                                  │
│  Memoria limpia, recursos liberados                            │
└─────────────────────────────────────────────────────────────────┘

════════════════════════════════════════════════════════════════════
                    FIN DE LA APLICACIÓN
════════════════════════════════════════════════════════════════════
```

## 🔑 Puntos Clave

### 1️⃣ Lifespan = Gerente de Recursos
```
┌─────────────────┐
│  LIFESPAN       │
│  ─────────────  │
│  • Crea: 1x     │  ← Al iniciar
│  • Usa: ∞       │  ← Durante ejecución  
│  • Destruye: 1x │  ← Al cerrar
└─────────────────┘
```

### 2️⃣ Dependency = Distribuidor de Recursos
```
┌───────────────────────────┐
│  DEPENDENCY INJECTION     │
│  ───────────────────────  │
│  Request 1 → Session A    │
│  Request 2 → Session B    │
│  Request 3 → Session C    │
│  (Todas usan mismo        │
│   adapter del lifespan)   │
└───────────────────────────┘
```

### 3️⃣ Relación entre Ambos
```
           LIFESPAN
              │
    ┌─────────┴─────────┐
    │                   │
    ▼                   ▼
ADAPTER (1x)        ENGINE (1x)
    │                   │
    └─────────┬─────────┘
              │
    ┌─────────┴─────────┬─────────┬─────────┐
    │                   │         │         │
    ▼                   ▼         ▼         ▼
Session 1           Session 2  Session 3  Session N
(Request 1)         (Request 2)(Request 3)(Request N)
    │                   │         │         │
    ▼                   ▼         ▼         ▼
 Endpoint            Endpoint  Endpoint  Endpoint
```

## 📊 Tabla Comparativa

| Aspecto | Lifespan | Dependency Injection |
|---------|----------|---------------------|
| **Se ejecuta** | 2 veces (inicio + fin) | N veces (cada request) |
| **Crea** | Adapter/Engine pesado | Session ligera |
| **Tiempo** | ~50ms una vez | ~1ms por request |
| **Memoria** | Un objeto compartido | Un objeto por request |
| **Propósito** | Inicializar recursos | Distribuir recursos |
| **Lifetime** | Toda la aplicación | Solo el request |

## 🎯 Analogía Final: La Pizzería

```
🍕 Pizzería FastAPI

LIFESPAN (Dueño):
├─ Compra el horno (una vez) ← Costoso
└─ Al cerrar, vende el horno (una vez)

DEPENDENCY INJECTION (Cocinero):
├─ Request 1: Usa el horno → Pizza 1
├─ Request 2: Usa el horno → Pizza 2
└─ Request N: Usa el horno → Pizza N

¿Por qué no comprar un horno nuevo para cada pizza?
❌ Sería carísimo y lento
✅ Mejor: Comprar uno y reutilizarlo

Lo mismo con la base de datos:
❌ Crear adapter nuevo cada request = Lento
✅ Crear adapter una vez + reusar = Rápido
```

## ✅ Tu Código Ya Está Correcto

```python
# app/core/lifespan.py
@asynccontextmanager
async def lifespan(app: FastAPI):
    global _async_adapter
    _async_adapter = create_adapter()  # UNA VEZ
    yield
    await _async_adapter.dispose()  # UNA VEZ

# app/main.py
app = FastAPI(lifespan=lifespan)  # ← Conecta el lifespan

# app/shared/dependencies/sql.py
async def get_session():
    adapter = get_async_adapter()  # Obtiene el compartido
    async with adapter.get_session() as session:
        yield session  # Cada request tiene su session

# Tus endpoints (NO CAMBIAS NADA)
@router.get("/restaurants")
async def handler(session = Depends(get_session)):
    result = await session.exec(...)
    return result.all()
```

## 🎓 Resumen en 3 Líneas

1. **Lifespan** = Crea el adapter UNA VEZ (al iniciar)
2. **Dependency** = Usa ese adapter en CADA REQUEST (da sessions)
3. **Endpoint** = Recibe la session y trabaja normal

¡Ya lo tienes funcionando! 🎉

