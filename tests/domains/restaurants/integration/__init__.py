"""Integration tests package for restaurants domain.

Contains integration tests that test multiple components working together
(e.g., repository + database, service + repository) with real dependencies.
"""
